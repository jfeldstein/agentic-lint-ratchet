# Ratchet queue: templates/** is Helm rendering, not portable YAML — keep out of yamllint until
# a Helm-aware check exists. values.yaml + chart metadata are linted via npm run lint:yaml.

extends: default

rules:
  document-start: disable
  line-length:
    max: 120
  truthy:
    check-keys: false
